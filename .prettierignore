# Lockfile — never reformatted
pnpm-lock.yaml

# registry.yaml's textual form is part of a release gate: tests regex-match the
# double-quoted `release_hash: "..."` line, so no formatter may touch it
registry.yaml

# Generated trees — regenerated by pnpm build; formatting must not drift
# (apps/web/src/worker carries a generated redirect table and bundles into the
# committed apps/web/public/_worker.js)
apps/web/public
apps/web/src/worker
schemas
data
formal
migration
migrations

# Build output, caches, tool state
**/dist
apps/web/.astro
apps/web/.output
coverage
test-results
.wrangler
.worktrees
.playwright-mcp
.claude
.agents

# Markdown is authored content, not tool-formatted code
**/*.md
docs
spec