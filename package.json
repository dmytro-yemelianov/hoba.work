{
  "name": "hoba-monorepo",
  "version": "0.6.0",
  "private": true,
  "description": "Hiring Obstacles & Barriers Atlas — canonical repo for hoba.work",
  "license": "MIT",
  "author": "Dmytro Yemelianov <dmytro@hoba.work>",
  "homepage": "https://hoba.work",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/dmytro-yemelianov/hoba.work.git"
  },
  "scripts": {
    "build": "pnpm build:packages && pnpm build:bodies && pnpm build:registry && pnpm generate:redirects && pnpm build:worker && pnpm build:discovery && pnpm --filter hoba-site build && pnpm build:cards",
    "build:bodies": "tsx scripts/build-bodies.ts",
    "build:cards": "tsx scripts/build-cards.ts",
    "build:discovery": "tsx scripts/build-discovery.ts",
    "build:lean": "tsx scripts/build-lean.ts",
    "build:packages": "tsc -b packages/registry-core packages/graph packages/validator packages/search packages/registry packages/cli packages/mcp && chmod +x packages/cli/dist/cli.js packages/mcp/dist/index.js",
    "build:registry": "tsx scripts/build-registry.ts",
    "build:worker": "tsx scripts/build-worker.ts",
    "clean": "rimraf node_modules packages/*/dist packages/*/node_modules packages/*/tsconfig.tsbuildinfo apps/web/.astro apps/web/dist apps/web/node_modules",
    "deploy:pages": "pnpm build && wrangler pages deploy apps/web/dist --project-name hoba-work --branch main --commit-dirty=true",
    "deploy:preview": "pnpm build && wrangler pages deploy apps/web/dist --project-name hoba-work --branch \"$(git branch --show-current)\" --commit-dirty=true",
    "dev": "pnpm --filter hoba-site dev",
    "e2e": "playwright test",
    "e2e:ui": "playwright test --ui",
    "generate:id-mapping": "tsx scripts/generate-id-mapping.ts",
    "generate:redirects": "tsx scripts/generate-redirects.ts",
    "lean": "pnpm build:lean && cd formal/lean && lake build",
    "ralph:eval": "tsx scripts/ralph-loop.ts",
    "rename-entities": "tsx scripts/rename-entities.ts",
    "task": "node scripts/task.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "pnpm build:packages && tsc -p tsconfig.json --noEmit && pnpm --filter hoba-site exec astro check --minimumSeverity error",
    "validate": "pnpm build:packages && tsx scripts/validate.ts",
    "validate:strict": "pnpm build:packages && tsx scripts/validate.ts --strict"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.13.0",
    "@fontsource/inter": "^5.3.0",
    "@hoba/registry": "workspace:*",
    "@playwright/test": "^1.62.1",
    "@resvg/resvg-js": "^2.6.2",
    "@types/node": "^22.13.4",
    "esbuild": "^0.28.2",
    "rimraf": "^6.0.1",
    "satori": "^0.33.4",
    "tsx": "^4.19.3",
    "typescript": "^5.7.3",
    "vitest": "^3.0.6",
    "wrangler": "4.112.0",
    "zod": "^3.24.2",
    "zod-to-json-schema": "^3.24.3"
  },
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=20"
  }
}
