{
  "name": "hoba-monorepo",
  "version": "0.4.1",
  "private": true,
  "description": "Hiring Obstacles & Barriers Atlas \u2014 canonical repo for hoba.work",
  "license": "MIT",
  "author": "Dmytro Yemelianov <dmytro@hoba.work>",
  "homepage": "https://hoba.work",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/dmytro-yemelianov/hoba.work.git"
  },
  "scripts": {
    "build": "pnpm build:packages && pnpm build:registry && pnpm build:discovery && pnpm --filter hoba-site build && pnpm build:cards",
    "build:packages": "tsc -b packages/registry packages/cli packages/mcp && chmod +x packages/cli/dist/cli.js packages/mcp/dist/index.js",
    "build:registry": "tsx scripts/build-registry.ts",
    "dev": "pnpm --filter hoba-site dev",
    "test": "vitest run",
    "test:watch": "vitest",
    "validate": "tsx scripts/validate.ts",
    "validate:strict": "tsx scripts/validate.ts --strict",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -b packages/registry packages/cli packages/mcp --dry",
    "deploy:pages": "pnpm build && wrangler pages deploy site/dist --project-name hoba-work --branch main",
    "clean": "rimraf node_modules packages/*/dist packages/*/node_modules packages/*/tsconfig.tsbuildinfo site/.astro site/dist site/node_modules",
    "deploy:preview": "pnpm build && wrangler pages deploy site/dist --project-name hoba-work --branch \"$(git branch --show-current)\"",
    "e2e": "playwright test",
    "e2e:ui": "playwright test --ui",
    "task": "node scripts/task.mjs",
    "build:discovery": "tsx scripts/build-discovery.ts",
    "build:cards": "tsx scripts/build-cards.ts"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.13.0",
    "@fontsource/inter": "^5.3.0",
    "@hoba/registry": "workspace:*",
    "@playwright/test": "^1.62.1",
    "@resvg/resvg-js": "^2.6.2",
    "@types/node": "^22.13.4",
    "rimraf": "^6.0.1",
    "satori": "^0.33.4",
    "tsx": "^4.19.3",
    "typescript": "^5.7.3",
    "vitest": "^3.0.6",
    "wrangler": "4.112.0",
    "zod": "^3.24.2",
    "zod-to-json-schema": "^3.24.3"
  },
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=20"
  }
}
